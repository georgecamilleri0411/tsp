import java.lang.System;public class tsp {	public static void main(String[] args) {		FileReader.readFile("trainfile3.txt");		int x1 = 0;		int y1 = 0;		// TEST START : using Nanotime		long start = System.nanoTime();		System.out.println ("Nanotime (start): " + start);		// TEST END: using Nanotime		for (City cFrom : FileReader.cities) {			x1 = cFrom.getX();			y1 = cFrom.getY();			for (City cTo : FileReader.cities) {				if (cFrom.getIndex() != cTo.getIndex()) {					System.out.println ("Distance from City " + cFrom.getIndex() + " to City " + cTo.getIndex()							+ " : " + Utilities.getEuclideanDistance(x1, y1, cTo.getX(), cTo.getY()));				}			}		}		// TEST START : using Nanotime		long end = System.nanoTime();		System.out.println ("Nanotime (end): " + end);		System.out.println ("Runtime: " + (end - start) + " nanoseconds");		// TEST END: using Nanotime	}}